# Security Policy

Report suspected vulnerabilities through GitHub's private security reporting
for `tenstorrent/tt_transformers`; do not open a public issue containing
exploit details, credentials, model access tokens, or proprietary checkpoints.

The package never bundles firmware, drivers, model weights, tokenizers, or
caches. Only versions explicitly listed in current support manifests and
qualification reports are considered supported.
